$('#<%= dom_id @section %>').html('<%=j render('form') %>');
bindNotice('form[data-remote]', 'Saving ...');
$('form img').click(function() {
  $(this).siblings('input[type=file]').click();
});
$('textarea').tinymce({
  content_css: '/assets/content.css',
  theme: 'advanced',
  theme_advanced_buttons1: 'bold,italic,strikethrough,separator,bullist,numlist,separator,undo,redo,separator,link,unlink',
  theme_advanced_buttons2: '',
  theme_advanced_buttons3: ''
});
