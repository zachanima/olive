<div id="<%= dom_id section %>" data-section="<%= section.to_param %>">
  <%= link_to '', edit_page_section_path(@page, section), class: 'edit' %>
  <%= link_to "\u2716", page_section_path(@page, section), method: 'delete', class: 'delete', confirm: 'Delete section?' %>
  <%= content_tag :h2, section.title unless section.title.blank? %>
  <%= section.text %>
</div>
