<div>
  <%= link_to '', edit_page_path(@page), class: 'edit' %>
  <h1><%= @page.title %></h1>
  <%= @page.text %>
</div>

<%= render @page.sections %>

<%= link_to 'New section', new_page_section_path(@page), id: 'new_section' %>
