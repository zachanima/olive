<div>
  <%= link_to '', edit_page_path(@page), class: 'edit' %>
  <h1><%= @page.title %></h1>
  <%= @page.text %>
</div>

<%= render @page.sections %>

<div id="new_section" data-page="<%= @page.to_param %>">
  <%= link_to 'New section', new_page_section_path(@page), class: 'edit' %>
</div>
